/**
 * SAML Strategy Stub
 *
 * This file is a placeholder for the generic SAML SSO strategy.
 * The actual implementation requires the `@node-saml/passport-saml` package.
 *
 * To enable SAML SSO:
 * 1. Run: pnpm add @node-saml/passport-saml --filter api
 * 2. Uncomment the SamlStrategy provider in auth.module.ts
 * 3. Implement the strategy using @node-saml/passport-saml
 *
 * See google-saml.strategy.ts for a reference implementation.
 */

// Stub export to prevent TypeScript errors
export class SamlStrategy {}
